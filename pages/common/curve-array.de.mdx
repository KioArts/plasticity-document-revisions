import VideoPlayer from '/components/VideoPlayer';
import Reference from '/components/Reference'
import Kst from '/components/KeyboardShortcutsTitle'
import { Callout } from 'nextra/components'

# Curve Array

<Reference shortcut="" command="Curve Array" button="" />

Duplizieren Sie Objekte entlang einer Kurve, indem Sie die Anzahl der Kopien festlegen. Optionen für Drehen (Twist), Skalieren, Ausrichtung und das Erstellen von Instanzen stehen zur Verfügung.

<Callout>
Bilder und Netze (.OBJ, .STL, .FBX usw.) können angeordnet werden, mit Steuerungen für Drehung, Skalierung, Ausrichtung und Platzierung entlang der Kurve. Das Instanziieren davon wird jedoch nicht unterstützt.
</Callout>

## Grundlegende Nutzung

1. Wählen Sie das Objekt aus, das Sie duplizieren möchten.
2. Verwenden Sie den **Curve Array** Befehl in der Befehlspalette.
3. Wählen Sie die Kurve aus, die als Pfad verwendet werden soll.
4. Legen Sie die Optionen im Befehlsdialog und den Tastaturkürzeln fest.
   - <Kst>Befehlsdialog</Kst>
      - **Number:** Geben Sie die Anzahl der Duplikationen an.
      - **Twist:** Geben Sie den Verdrehungswinkel entlang der Kurve in Grad an.
      - **Scale:** Geben Sie den Skalierungsfaktor entlang der Kurve an.
       - **Alignment:**
         - **Parallel:** Duplizieren Sie die Kurven parallel, unabhängig vom Pfad.
         - **Normal:** Duplizieren Sie die Kurven mit Winkeln, die an den Pfad ausgerichtet sind.
      - **Extent:** Geben Sie einen Wert an, um den Duplikationsbereich basierend auf der aktuell ausgewählten Methode anzupassen.
      - **Method:** Legt fest, wie der Duplikationsbereich gesteuert wird.
         - **Distance:** Passt den Duplikationsbereich mit einem metrischen Wert an, basierend auf den Einstellungen in Plasticity.
         - **Ratio:** Passt den Duplikationsbereich mit einem Verhältniswert zwischen 0,00 und 1,00 an. (Standard: 1,00)
      - **Make instances:** Create instances instead of independent copies. (See [Create Instances](/solid/create-instance))
   - <Kst>Tastenkombinationen</Kst>
      - <kbd>Umschalt + Rad hoch</kbd> **Add copy:** Erhöht die Anzahl der Duplikationen.
      - <kbd>Umschalt + Rad runter</kbd> **Subtract copy:** Verringert die Anzahl der Duplikationen.
4. Bestätigen Sie mit OK im Befehlsdialog oder mit einem Rechtsklick.

<VideoPlayer fileName="curve-array"/>

## Siehe auch
- [Rectangular Array](rectangular-array)
- [Radial Array](radial-array)