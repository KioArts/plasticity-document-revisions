import { Callout } from 'nextra/components'
import Reference from '/components/Reference'
import Kst from '/components/KeyboardShortcutsTitle'
import { Tabs } from 'nextra/components'

# シェーダーモード

## シェーダーモード
- Plasticityの内蔵シェーダーをクリックすることで切り替えることができます。[レンダーモード](/plasticity-essentials/view-mode)がオフの場合、選択されたPlasticityのマットキャップがオブジェクトに適用され表示されます。

- 使用可能なマットキャップ:

![シェーダーモードのマットキャップ](/img/shader-mode-preview.png)

| 名前 | 番号 | 適用範囲 | 注意事項 |
| ---- | ------ | ------------- | ---- |
| ダークセラミック | 1 | デフォルトの外観 | - |
| メタルカーペイント | 2 | 光沢のある外観 | - |
| ダークシャイニー | 3 | 光沢のある外観 | - |
| グラデーション | 4 | 環境グラデーションの外観 | - |
| セラミック電球 | 5 | ハイライトチェッカー | [レンダーモード](/plasticity-essentials/render-mode)がオフの場合、異なるマテリアルカラーをリアルタイムでビューポートに表示できます。 |
| カラーマットキャップ | 6 | マットな外観 | [レンダーモード](/plasticity-essentials/render-mode)がオフの場合、異なるマテリアルカラーをリアルタイムでビューポートに表示できます。 |
| ブラックシルエット | 7 | シルエットチェッカー | - |
| カラーシルエット | 8 | カラー付きシルエットチェッカー | [レンダーモード](/plasticity-essentials/render-mode)がオフの場合、異なるマテリアルカラーをリアルタイムでビューポートに表示できます。 |
| アイソフォート/アイソアングル | 9 | 静的な表面連続性チェッカー | 現在のカメラアングルに基づいてシェーダーがオブジェクトに適用されます。**「マテリアルオプション」**メニューでストライプの密度を調整できます。 |
| 抜き勾配チェッカー | 10 | 成形用抜き勾配チェッカー | 角度は現在の作業平面に基づいてオブジェクトに表示されます。**「マテリアルオプション」**メニューで抜き勾配の閾値（青色）を調整できます。 |
| Horizontale Zebra-Reflexion | 11 | Oberflächenkontinuitäts-Checker | Adjust the number of zebra stripes in the **"Material Options"** menu. |
| Vertikale Zebra-Reflexion | 12 | Oberflächenkontinuitäts-Checker | Adjust the number of zebra stripes in the **"Material Options"** menu. |
| Topology Shader    |  13    |  Surface Checker                 |  Use on imported mesh files to trace or create a wireframe look for reverse engineering purposes. This is useful for analyzing complex organic shapes with intricate curves and deformations, which may not be as easily visible when using highlighters or other shaders. You can adjust the direction and line distribution in the **"Material Options"** panel. |
| Normal Checker     |  14    |  Normal Surface Checker                 |  Use this shader to check if any surfaces are reversed. Blue indicates that the surface is facing outward, while red indicates it is facing inward. |

## Ambient Occlusion (AO)

Ambient Occlusion is turned off by default. However, you can enable it in the Performance section in the [Preferences](/plasticity-essentials/preferences) menu. Enabling AO is recommended for machines with high-end GPUs. You can adjust its appearance such as the radius (1) and falloff (2) in the Appearance section in the Preferences menu.

![Ambient Occlusion Settings](/img/ao-settings.png)

## すべてのマットキャップ
- カスタムマットキャップは「すべてのマットキャップ」カテゴリに表示されます。

![カスタムマットキャップ](/img/custom-matcaps-preview.png)

### カスタムマットキャップのインストール

1. カスタムマットキャップのEXRおよびPNGファイルが同じ名前になっていることを確認します。
2. `.plasticity` ディレクトリを探し、「matcaps」フォルダを作成します。

<Tabs items={['Windows', 'macOS', 'Linux']}>
    <Tabs.Tab>
    `C:/Users/<username>/.plasticity` に移動します。
    </Tabs.Tab>
    <Tabs.Tab>
    `~/.plasticity` に移動します。
    </Tabs.Tab>
    <Tabs.Tab>
    `C:/usr/lib/plasticity/resources/app/.webpack/main/dot-plasticity` に移動します。
    </Tabs.Tab>
</Tabs>

3. カスタムマットキャップファイルをこのフォルダに移動し、Plasticityを再起動します。

![マットキャップのインストール](/img/installing-matcaps-preview.png)

<Callout type="info" emoji="💡">
**ヒント**     
- EXRおよびPNGファイルのサイズは512x512ピクセルで、透明な背景を持つ必要があります。EXRファイルは少なくとも16ビットカラーであることを確認してください。
</Callout>

<Callout emoji="❗">
**Note**     
- オペレーティングシステムで隠しフォルダーを表示するオプションを有効にして、`.plasticity` ディレクトリにアクセスできるようにしてください。
</Callout>

### エッジ、カーブ、フェイスの表示切り替え

- エッジ、カーブ、フェイスはトグルボタンで表示/非表示を切り替えることができます。

![すべてのマットキャップのトグルオプション](/img/toggle-edges-curves-faces.png)

| 機能 | ショートカット | ボタン | コマンド名 |
| ---- | -------- | ------ | --------- |
| エッジの表示切り替え <br />(オフにするとエッジが非表示になります) | | (13) | ビューポート: エッジの表示切り替え |
| カーブの表示切り替え <br />(オフにするとカーブが非表示になります) | | (14) | ビューポート: カーブの表示切り替え |
| フェイスの表示切り替え <br />(オフにするとフェイスが非表示になります) | | (15) | ビューポート: フェイスの表示切り替え |

### See Also
[Render Mode](/plasticity-essentials/render-mode)